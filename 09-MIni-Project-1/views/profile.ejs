<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-zinc-900 min-h-screen flex items-center justify-center">
    <div class="w-full max-w-6xl p-4">
      
      <div class="flex justify-between items-center mb-6">
        <div class="flex  gap-2">
          <div class="w-10 h-10 rounded-md overflow-hidden">
            <img class="w-full h-full object-cover" src="\file\uploads\<%=user.profilePic%>" alt="">
          </div>
        <h3
          class="text-xl font-bold flex text-white items-center"
        >
          Hello <%= user.name %> 👋
        </h3>
        </div>

        <a href="/logout" class="text-indigo-400 hover:text-indigo-300">
          logout
        </a>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
        
        <div>
          <h5 class="text-xl text-zinc-400 mb-4">
            you can create a new post
          </h5>

          <form
            action="/post"
            method="post"
            class="bg-zinc-800 p-4 rounded-lg shadow-lg"
          >
            <textarea
              name="content"
              rows="5"
              placeholder="What's on your mind?"
              class="resize-none w-full bg-zinc-900 text-white border-2 border-zinc-700 rounded-md p-3 focus:outline-none focus:border-indigo-500 transition"
            ></textarea>

            <button
              type="submit"
              class="mt-4 w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-2 px-4 rounded-md transition"
            >
              Post
            </button>
          </form>
        </div>

        <div class="posts">
          <h3 class="text-2xl font-semibold text-white mb-4">
            Your Posts
          </h3>

          <% user.posts.reverse().forEach(function(post){ %>
            <div class="bg-zinc-800 p-4 rounded-lg shadow-lg mb-4">
              
              <p class="text-white font-semibold mb-2">
                @<%= user.username %>
              </p>

              <p class="text-zinc-300 mb-3">
                <%= post.content %>
              </p>

              <div class="flex justify-end items-center text-sm">
                <small class="text-zinc-500 mr-4">
                  <%= post.likes.length %> Likes
                </small>

                <a
                  href="/like/<%= post._id %>"
                  class="text-zinc-500 hover:text-indigo-400 mr-4"
                >
                  <%= post.likes.indexOf(user._id) === -1 ? 'Like' : 'Unlike' %>
                </a>

                <a
                  href="/edit/<%= post._id %>"
                  class="text-zinc-500 hover:text-indigo-400"
                >
                  Edit
                </a>
                <a
                  href="/delete/<%= post._id %>"
                  class="text-zinc-500 hover:text-indigo-400"
                >
                  Delete
                </a>
              </div>
            </div>
          <% }) %>
        </div>

      </div>
    </div>
  </body>
</html>
