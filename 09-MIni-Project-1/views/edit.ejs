<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-zinc-900 flex flex-col items-center justify-center min-h-screen"
  >
    <div class="flex flex-col items-center justify-center w-full max-w-md p-4">
      <div class="w-full">
        <div class="flex justify-end mb-4">
          <a href="/logout" class="text-indigo-400 hover:text-indigo-300"
            >logout</a
          >
        </div>
        <h3
          class="text-xl font-bold text-white p-4 bg-zinc-800 rounded-lg shadow-lg mb-6 w-full text-center"
        >
          Hello <%=post.user.name%> 👋
        </h3>
      </div>

      <h5 class="text-xl text-zinc-400 mb-4 self-start">you can edit a post</h5>
      <form
        action="/update/<%=post._id%>"
        method="post"
        class="w-full max-w-lg p-4 bg-zinc-800 rounded-lg shadow-lg mb-8"
      >
        <textarea
          name="content"
          class="resize-none w-full bg-zinc-900 text-white border-2 border-zinc-700 rounded-md p-3 focus:outline-none focus:border-indigo-500 transition duration-150"
          rows="5"
          placeholder="What's on your mind?"
        >
    <%=post.content%></textarea
        >
        <button
          type="submit"
          class="mt-4 w-full bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 text-white font-bold py-2 px-4 rounded-md transition duration-150"
        >
          Post
        </button>
      </form>
    </div>
  </body>
</html>
