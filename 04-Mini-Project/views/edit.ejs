<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Task</title>

    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>

  <body class="bg-zinc-900 text-white">
    <div class="min-h-screen flex items-center justify-center px-4">
      <div class="w-full max-w-3xl bg-zinc-800 p-8 rounded-2xl shadow-xl">
        <div class="flex justify-between items-center mb-6">
          <h1 class="text-2xl font-semibold">Edit Task ✏️</h1>

          <a
            href="/"
            class="text-sm px-4 py-2 rounded-lg bg-zinc-700 hover:bg-zinc-600 transition"
          >
            ⬅ Back
          </a>
        </div>

        <form method="POST" action="/edit/<%=filename%>" class="space-y-5">
          <div>
            <label class="block text-sm text-zinc-400 mb-1">
              Current Task Name
            </label>

            <input
              type="text"
              value="<%= filename %>"
              name="oldTitle"
              disabled
              class="w-full px-4 py-2 rounded-lg bg-zinc-700/60 text-zinc-400 cursor-not-allowed"
            />
          </div>

          <div>
            <label class="block text-sm text-zinc-400 mb-1">
              New Task Name
            </label>

            <input
              type="text"
              name="newTitle"
              placeholder="Enter new task name"
              required
              class="w-full px-4 py-2 rounded-lg bg-zinc-700 placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-500"
            />
          </div>

          <div class="flex justify-end gap-3 pt-4">
            <a
              href="/"
              class="px-4 py-2 rounded-lg bg-zinc-700 hover:bg-zinc-600 transition"
            >
              Cancel
            </a>

            <button
              type="submit"
              class="px-6 py-2 rounded-lg bg-blue-600 hover:bg-blue-700 active:scale-95 transition"
            >
              💾 Save Changes
            </button>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
