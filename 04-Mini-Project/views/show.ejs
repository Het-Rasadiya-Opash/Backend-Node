<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Task Details</title>

    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>

  <body class="bg-zinc-900 text-white">
    <div class="min-h-screen flex items-center justify-center px-4">
      <div
        class="w-full max-w-4xl bg-zinc-800 p-8 rounded-2xl shadow-xl animate-fade-in"
      >
        <div class="flex justify-between items-center mb-6 gap-4">
          <h1 class="text-2xl font-semibold">Task Details 📄</h1>

          <div
            class="px-4 py-1 rounded-lg bg-zinc-700 text-sm text-zinc-300 font-mono truncate max-w-xs"
            title="<%= fileName %>"
          >
            📄 <%= fileName %>
          </div>

          <a
            href="/"
            class="text-sm px-4 py-2 rounded-lg bg-zinc-700 hover:bg-zinc-600 transition"
          >
            ⬅ Back
          </a>
        </div>

        <div
          class="bg-zinc-700 p-6 rounded-xl text-zinc-200 leading-relaxed whitespace-pre-wrap"
        >
          <%- fileData %>
        </div>
      </div>
    </div>
  </body>
</html>
